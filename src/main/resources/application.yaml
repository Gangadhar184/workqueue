spring:
  application:
    name: workqueue
  redis:
    host: localhost
    port: 6379


management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
